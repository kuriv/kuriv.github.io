# 正则约束

你可以通过使用下面的方法来约束路由参数的格式。

```php
<?php

Route::get('user/{id}', function ($id) {
    return $id;
})->where('id', '[0-9]+');

Route::get('user/{id}/{name}', function ($id, $name) {
    return $id . '-' . $name;
})->where([
    'id'   => '[0-9]+',
    'name' => '[a-zA-Z]+'
]);

```

